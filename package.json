{
  "name": "dynschedule",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "pnpm run client",
    "server": "vercel dev --listen 3001",
    "client": "cd client && cross-env SCHOOL_ASSIGNMENT=salesian HOSTER=localhost NODE_ENV=development pnpm run dev --host"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5",
    "cross-env": "^7.0.3",
    "vercel": "^37.4.0"
  }
}
